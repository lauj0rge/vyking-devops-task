mysql:
  architecture: replication

  auth:
    existingSecret: mysql-auth
    username: produser
    database: proddb

  primary:
    persistence:
      size: 20Gi
    service:
      port: 3306   # standard MySQL port

  secondary:
    replicaCount: 2

  resources:
    requests:
      cpu: 500m
      memory: 512Mi
    limits:
      cpu: 2
      memory: 2Gi

commonLabels:
  environment: prod
