mysql:
  architecture: standalone

  auth:
    existingSecret: mysql-auth
    username: devuser
    database: devdb

  primary:
    persistence:
      size: 1Gi
    service:
      port: 3307   # dev port to avoid clash with prod

  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi

commonLabels:
  environment: dev
